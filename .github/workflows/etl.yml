name: ETL Pipeline

on:
  schedule:
    - cron: "0 0 * * *"   # 09:00 JST
    - cron: "0 3 * * *"   # 12:00 JST
    - cron: "0 6 * * *"   # 15:00 JST
  workflow_dispatch:       # 手動実行も可能

jobs:
  etl:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Write service account key
        run: |
          mkdir -p credentials
          echo '${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}' > credentials/gcp-secret-key.json

      - name: Write LINE WORKS secret
        run: |
          mkdir -p credentials
          echo '${{ secrets.LINEWORKS_API_KEY }}' > credentials/lineworks_secret.json

      - name: Run ETL
        run: python main.py

      - name: Notify on workflow failure
        if: failure()
        run: |
          python -c "
          from etl.lineworks import send_message
          send_message('ETL Pipeline ワークフローが失敗しました: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}')
          "
